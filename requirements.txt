# torch/torchaudio 2.7: torchaudio.load() here decodes via the ffmpeg backend, NOT
# torchcodec. That avoids the torchcodec<->CUDA version mismatch on the Space's GPU
# (torchcodec 0.14 wants CUDA 13 / libnvrtc.so.13, but torch ships CUDA 12.8). ffmpeg
# comes from packages.txt. f5-tts 1.1.20 still exposes the get_input_embed DiT API the
# LoRA was trained against. README.md pins python_version: 3.11.
torch==2.7.1
torchaudio==2.7.1
f5-tts==1.1.20
peft==0.18.1
gradio==6.10.0
gradio_client==2.4.0
transformers
datasets
huggingface_hub
soundfile
numpy
scipy
librosa
g2p_en
panphon
jiwer
openai-whisper
resemblyzer
wordfreq
fastapi
uvicorn
pedalboard

